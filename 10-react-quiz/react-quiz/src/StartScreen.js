function StartScreen({ total }) {
  return (
    <div>
      <h2>Welcome to The React Quiz</h2>
      <h3> {total} question to test your react mastery</h3>
      <button>Let's start</button>
    </div>
  );
}

export default StartScreen;
